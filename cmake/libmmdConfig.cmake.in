@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

if(NOT WIN32)
    find_dependency(Iconv)
endif()

if(@LIBMMD_CONFIG_NEEDS_BULLET@)
    find_dependency(Bullet)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/libmmdTargets.cmake")
